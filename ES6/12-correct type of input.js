if(!Array.isArray(a) || !a.every(i => typeof i == "number"))
        return "invalid input";


// !Array.isArray(a)
//    Array.isArray(a) checks if a is an array.

// a.every(i => condition)
//     checks every element inside a.

// typeof i === "number" 
//     ensures each element is a number.
